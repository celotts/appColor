export const environment = {
  production: true,

  apiUrl: 'https://reqres.in/api/',

  service: {
    colors: {
      public: false,
      nameservice: 'colors',
      endpoint: 'colors'

    }
  }
};
